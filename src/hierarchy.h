#ifndef HIERARCHY_H
#define HIERARCHY_H

#include <vector>

// One single definition of Hierarchy, shared by all implementations.
struct Hierarchy {
  std::vector<std::vector<int>> partitions;
};

#endif // HIERARCHY_H
